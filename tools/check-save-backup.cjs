const path=require('path'),assert=require('node:assert/strict');const {chromium}=require(path.join(process.env.AUDIT_NODE_MODULES,'playwright'));
(async()=>{const b=await chromium.launch({channel:'msedge',headless:true});try{const p=await b.newPage({acceptDownloads:true}),errors=[];p.on('pageerror',e=>errors.push(e.message));await p.goto('http://127.0.0.1:5512');await p.waitForFunction(()=>window.game,null,{timeout:90000});await p.locator('#title .go').click();await p.evaluate(()=>{game.dialogue.close();game.settings.setOpen(true)});const [d]=await Promise.all([p.waitForEvent('download'),p.getByRole('button',{name:'기록 파일로 보관'}).click()]);const fs=require('fs');const data=JSON.parse(fs.readFileSync(await d.path(),'utf8'));assert.equal(data.version,1);assert(data.progress.lab);assert.deepEqual(errors,[]);console.log('PASS settings backup downloads valid game envelope');}finally{await b.close()}})().catch(e=>{console.error(e);process.exitCode=1});
